"""
app/utils/__init__.py - Utility / Helper Functions Package
===========================================================
WHY THIS FOLDER EXISTS:
  Houses small, reusable helper functions that don't belong to any
  single service or route — e.g. input validators, formatters,
  file-type checkers, decorators. Prevents code duplication.
"""
